#ifndef FILTER_DATA_POINTS_H
#define FILTER_DATA_POINTS_H

#include <vector>
#include <rarray>

std::vector<double> filter_data_points(const std::vector<double>& dataPoints, int rank, int size);

#endif
